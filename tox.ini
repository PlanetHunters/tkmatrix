[tox]
envlist = py{38,39}
[testenv]
wheel = true
deps=
    numpy
    pytest
    setuptools
    wheel
    Cython
whitelist_externals = pytest
    /bin/rm
commands =
    pytest -v tkmatrix/tests/
    rm .tox/{envname} -R
